meta {
  name: Get Orders
  type: http
  seq: 1
}

get {
  url: {{base_url}}/orders
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

docs {
  Get list of orders (user sees own orders, admin sees all)
}